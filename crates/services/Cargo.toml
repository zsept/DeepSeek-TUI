[package]
name = "deepseek-services"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "DeepSeek runtime services: API server, task/automation/thread management"

[dependencies]
deepseek-shared = { path = "../shared", version = "0.8.38" }
deepseek-engine = { path = "../engine", version = "0.8.38" }
